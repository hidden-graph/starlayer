Task List

Comments on user manaual.

	- SPARQL section should include additional supported terms: TRIPLE, SUBJECT, PREDICATE, OBJECT, LANGDIR, hasLANGDIR, hasLANG, STRLANGDIR.
- user of ttl annotation syntax inside sparql queries.
	- Reification helper coverage should include reifiers/reifications/reifier_annotations/reified_triples/remove_reification.
- obtain the rdf of a SPARQL query.  
- obtain the SHACL shape for the SPARQL query

SHACL expansion notes (new):

- Add manual coverage for SHACL 1.2 Node Expressions (shnex) and SPARQL node-expression support.
- Add manual coverage for SHACL 1.2 rule execution controls: sh:layer, sh:runOnce, sh:expectedPredicate, sh:tempTriple.

- Provide example of SHACL shape with UI elements. Clarify UI claim: shui metadata/presentation shapes are supported as metadata and meta-shape checks, but widget-selection runtime is not implemented.

- Add runnable SHACL examples for:
	1) validate with dirLangString constraints,
	2) apply_rules with inferred triples,
	3) one shnex expression example,
	4) one SPARQL node-expression example.





1. surpise, the jsonld12 serialization leaks the underlying rdf 1.1 terms



1. what art the git errors we get when we publish. how to fix

3. bring in OWL and OWL reasoning into the stack.
4a. break the sparql/shacl editor into two parts.  build shacl as stand-alone.1
4. rebuild a stand-alone SPARQL editor based on shapes.
5. create an HTML based user guide

7. do we have claude instructions to check for updates to the 1.2 standards
8. bring relevant UI apps into hiddenGrpah.  KG explorere, sparqModel
9. why are we still finding bugs? need to expand testing? different approach?

