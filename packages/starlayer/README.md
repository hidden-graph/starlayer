# starlayer

This package is the full Starlayer install: graph + SPARQL + SHACL.

## Install

```bash
pip install starlayer
```

This package is a convenience wrapper around the repo's packaged components:

- `graph`
- `sparql`
- `shacl`

It is intended for the public-facing full-stack install story.
